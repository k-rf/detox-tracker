import { DetoxTimer } from "./detox-timer";

import type { Meta, StoryObj } from "@storybook/react";

export default {
  component: DetoxTimer,
} as Meta<typeof DetoxTimer>;

type Story = StoryObj<typeof DetoxTimer>;

export const Default: Story = {};
