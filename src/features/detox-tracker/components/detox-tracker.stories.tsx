import { DetoxTracker } from "./detox-tracker";

import type { Meta, StoryObj } from "@storybook/react";

export default {
  component: DetoxTracker,
} as Meta<typeof DetoxTracker>;

type Story = StoryObj<typeof DetoxTracker>;

export const Default: Story = {};
